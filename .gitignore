# Archives and release assets
*.tar.gz
*.tgz
*.zip
*.sha256

# Runtime configs and secrets
.env
*.env
config.env
prefix.env
secure-notes/
config-backups/
manual-backups/

# Handoff: internal AI-agent context. Contains real fleet/box/domain data in places — NEVER commit
# the whole set (per the CLAUDE.md rule). Ignoring the entire dir prevents an accidental `git add -A`.
handoff/

# Local AI-agent context (points at the local handoff/ set — not for the public repo)
CLAUDE.md
CLAUDE.local.md

# Local install/build/test directories
dist/
build/
xpam-install/
xpam-v*/
xpam-*-build/
xpam-*-audit/
xpam-*-test/

# Logs and temporary files
*.log
*.tmp
*.bak
*.old
*.orig
*.swp
*~

# Python cache
__pycache__/
*.pyc

# OS/editor
.DS_Store
.vscode/
.idea/
